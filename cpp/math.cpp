#include <iostream>
#include <cmath>

using namespace std;

int main() {

  // max and min
  cout << max(5 ,10);
  cout << min(5 ,10);

  // <cmath> lib
  cout << sqrt(64);
  cout << round(2.6);
  cout << log(2);

  // other Math functions
  abs(x); // absolute value of x
  ceil(x); // rounded up
  floor(x) // rounded down


  return 0;
}